import React, { Component } from 'react';
import Formulario from './Formulario';

class Main extends Component {
  render() {
    return (
        <div>
            <Formulario/>

        </div>

    );
  }
}

export default Main;
